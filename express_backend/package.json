{
  "name": "express_backend",
  "version": "1.0.0",
  "description": "express server which will glue all the backend containers",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start-env": "PGHOST=localhost PGPORT=5432 PGDATABASE=pgbackenddb PGUSER=postgres PGPASSWORD=postgres node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "pg": "^7.8.0"
  }
}
