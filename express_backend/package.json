{
  "name": "express_backend",
  "version": "1.0.0",
  "description": "express server which will glue all the backend containers",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start-env": "PGHOST=localhost PGPORT=5432 PGDATABASE=postgres PGUSER=postgres PGPASSWORD=postgres_password node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "nodemon": "^1.18.10",
    "pg": "^7.8.0"
  }
}
